import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    addNoteBtn: {
        alignItems: "center",
        backgroundColor: 'gray'
    },
    wrapper: {
        flex:1
    }
});